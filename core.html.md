# dialoghelper


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

``` python
from dialoghelper import *
```

``` python
from fastcore import tools
from fastcore.test import *
```

## Basics

``` python
a = 1
find_var('a')
```

    1

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L65"
target="_blank" style="float:right; font-size:smaller">source</a>

### names_containing

``` python
def names_containing(
    s:str
):
```

*Names containing `s` in IPython user namespace, or caller globals*

``` python
names_containing('ipython')
```

    ['get_ipython', 'ipython_shell', 'in_ipython', 'load_ipython_extension']

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L80"
target="_blank" style="float:right; font-size:smaller">source</a>

### find_dname

``` python
def find_dname(
    dname:NoneType=None, required:bool=True
):
```

*Resolve `dname` relative to the current dialog’s folder, or from the
gateway root with a leading slash.*

``` python
find_dname()
```

    'aai-ws/dialoghelper/nbs/00_core.ipynb'

``` python
async def repro_executor():
    return await asyncio.get_running_loop().run_in_executor(None, find_dname)
await repro_executor()
```

    'aai-ws/dialoghelper/nbs/00_core.ipynb'

``` python
find_dname('index.ipynb')
```

    'aai-ws/dialoghelper/nbs/index.ipynb'

``` python
find_dname('../index.ipynb')
```

    'aai-ws/dialoghelper/index.ipynb'

``` python
find_dname('/foo/bar.ipynb')
```

    'foo/bar.ipynb'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L108"
target="_blank" style="float:right; font-size:smaller">source</a>

### xgeta

``` python
async def xgeta(
    url, **kwargs
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L105"
target="_blank" style="float:right; font-size:smaller">source</a>

### xposta

``` python
async def xposta(
    url, **kwargs
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L123"
target="_blank" style="float:right; font-size:smaller">source</a>

### DialogAPIError

``` python
def DialogAPIError(
    *args, **kwargs
):
```

*A Solveit API endpoint reported an error (its JSON response carried an
`error` key)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L139"
target="_blank" style="float:right; font-size:smaller">source</a>

### call_endpa

``` python
async def call_endpa(
    path, dname:str='', json:bool=False, raiseex:bool=False, id:NoneType=None, required:bool=True, timeout:int=10,
    audit:bool=False, chkerr:bool=True, **data
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L134"
target="_blank" style="float:right; font-size:smaller">source</a>

### call_endp

``` python
def call_endp(
    path, dname:str='', json:bool=False, raiseex:bool=False, id:NoneType=None, required:bool=True, timeout:int=10,
    audit:bool=False, chkerr:bool=True, **data
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L151"
target="_blank" style="float:right; font-size:smaller">source</a>

### curr_dialog

``` python
async def curr_dialog(
    dname:str='', # Dialog to get info for; defaults to current dialog
)->dict:
```

*Get the current dialog info.*

``` python
await curr_dialog()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L167"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_html

``` python
def add_html(
    content:str, # The HTML to send to the client (generally should include hx-swap-oob)
    dname:str='', # Dialog to get info for; defaults to current dialog
):
```

*Send HTML to the browser to be swapped into the DOM*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L159"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_html_a

``` python
async def add_html_a(
    content:str, # The HTML to send to the client (generally should include hx-swap-oob)
    dname:str='', # Dialog to get info for; defaults to current dialog
):
```

*Send HTML to the browser to be swapped into the DOM*

``` python
from fasthtml.common import *
```

``` python
add_html(Div(P('Hi'), hx_swap_oob='beforeend:#dialog-container'))
```

    {'success': 'Content added to DOM'}

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L181"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_scr

``` python
def add_scr(
    scr, oob:str='innerHTML:#ephemeral'
):
```

*Swap a script element to the end of the ephemeral element*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L176"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_scr_a

``` python
async def add_scr_a(
    scr, oob:str='innerHTML:#ephemeral'
):
```

*Swap a script element to the end of the ephemeral element*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L206"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_mod_a

``` python
async def add_mod_a(
    s:str
):
```

*Wrap javascript code string in a js script module and add it via
[`add_html`](https://AnswerDotAI.github.io/dialoghelper/core.html#add_html)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L202"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_mod

``` python
def add_mod(
    s:str
):
```

*Wrap javascript code string in a js script module and add it via
[`add_html`](https://AnswerDotAI.github.io/dialoghelper/core.html#add_html)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L198"
target="_blank" style="float:right; font-size:smaller">source</a>

### iife

``` python
def iife(
    code:str
):
```

*Wrap javascript code string in an IIFE and execute it via
[`add_html`](https://AnswerDotAI.github.io/dialoghelper/core.html#add_html)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L194"
target="_blank" style="float:right; font-size:smaller">source</a>

### iife_a

``` python
async def iife_a(
    code:str
):
```

*Wrap javascript code string in an IIFE and execute it via
[`add_html`](https://AnswerDotAI.github.io/dialoghelper/core.html#add_html)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L214"
target="_blank" style="float:right; font-size:smaller">source</a>

### pop_data

``` python
def pop_data(
    idx, timeout:int=15
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L211"
target="_blank" style="float:right; font-size:smaller">source</a>

### pop_data_a

``` python
async def pop_data_a(
    idx, timeout:int=15
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L224"
target="_blank" style="float:right; font-size:smaller">source</a>

### fire_event

``` python
def fire_event(
    evt:str, **data
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L223"
target="_blank" style="float:right; font-size:smaller">source</a>

### fire_event_a

``` python
async def fire_event_a(
    evt:str, **data
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L237"
target="_blank" style="float:right; font-size:smaller">source</a>

### event_get

``` python
def event_get(
    evt:str, timeout:int=15, **data
):
```

*Call
[`fire_event`](https://AnswerDotAI.github.io/dialoghelper/core.html#fire_event)
and then
[`pop_data`](https://AnswerDotAI.github.io/dialoghelper/core.html#pop_data)
to get a response*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L231"
target="_blank" style="float:right; font-size:smaller">source</a>

### event_get_a

``` python
async def event_get_a(
    evt:str, timeout:int=15, **data
):
```

*Call
[`fire_event`](https://AnswerDotAI.github.io/dialoghelper/core.html#fire_event)
and then
[`pop_data`](https://AnswerDotAI.github.io/dialoghelper/core.html#pop_data)
to get a response*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L244"
target="_blank" style="float:right; font-size:smaller">source</a>

### trigger_now

``` python
def trigger_now(
    evt, data:NoneType=None, ttl:int=5000
):
```

*Synchronously trigger a browser event, safe against replay*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L263"
target="_blank" style="float:right; font-size:smaller">source</a>

### event_once_a

``` python
async def event_once_a(
    evt, timeout:int=15, ttl:int=5000, **data
):
```

*Like event_get_a but with replay/dedup safety via trigger_now*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L257"
target="_blank" style="float:right; font-size:smaller">source</a>

### event_once

``` python
def event_once(
    evt, timeout:int=15, ttl:int=5000, **data
):
```

*Like event_get but with replay/dedup safety via trigger_now*

``` python
iife("$('body').one('test_evt', e => pushData(e.detail.idx, {reply: 'it worked!'}));")
event_once('test_evt', data={'hello': 'world'})
```

<script>
if (Date.now() - 1787533171792 < 5000 && !window["_trig_1787533171792"]) {
    window["_trig_1787533171792"]=1;
    htmx.trigger(document.body, 'test_evt', {"data": {"hello": "world"}, "idx": "f14d1951-7e8f-47a9-9189-6299b8bd0c87"});
}</script>

``` python
{'data_id': 'f14d1951-7e8f-47a9-9189-6299b8bd0c87', 'reply': 'it worked!'}
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L277"
target="_blank" style="float:right; font-size:smaller">source</a>

### js_run_a

``` python
async def js_run_a(
    code
):
```

*Run JS code that calls done() when finished, and wait for result*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L271"
target="_blank" style="float:right; font-size:smaller">source</a>

### js_run

``` python
def js_run(
    code
):
```

*Run JS code that calls done() when finished, and wait for result*

``` python
r = js_run("setTimeout(() => done({answer: 6*7}), 100)")
r['answer']
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L290"
target="_blank" style="float:right; font-size:smaller">source</a>

### js_eval_a

``` python
async def js_eval_a(
    expr
):
```

*Evaluate a JS expression in the browser and return the result*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L284"
target="_blank" style="float:right; font-size:smaller">source</a>

### js_eval

``` python
def js_eval(
    expr
):
```

*Evaluate a JS expression in the browser and return the result*

``` python
js_eval("return {width: window.innerWidth}")
```

``` python
{'data_id': 'ee43ab10-480d-4469-9c5a-b3a63eb6f3f8', 'result': {'width': 1374}}
```

``` python
js_eval("return document.querySelectorAll('.msg').length")
```

``` python
{'data_id': 'ad5692cc-097e-498c-9adb-fca12fb12b94', 'result': 0}
```

``` python
js_eval("a=1")
js_eval("return a+1")
```

``` python
{'data_id': '3b056d62-bc76-4b23-a0be-394a336edc7b', 'result': 2}
```

[`event_get`](https://AnswerDotAI.github.io/dialoghelper/core.html#event_get),
[`js_run`](https://AnswerDotAI.github.io/dialoghelper/core.html#js_run),
and
[`js_eval`](https://AnswerDotAI.github.io/dialoghelper/core.html#js_eval)
each collect one response from the browser page. Use
[`Channel`](https://AnswerDotAI.github.io/dialoghelper/core.html#channel)
for an ongoing exchange, such as an event stream or a protocol with
repeated requests. It keeps a two-way WebSocket connection open instead
of making an HTTP round trip for each message.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L297"
target="_blank" style="float:right; font-size:smaller">source</a>

### Channel

``` python
def Channel(
    chan, url:NoneType=None
):
```

*Duplex JSON messaging with the other peers on a named `/wsx` relay
channel*

Connect each peer to the same channel name on Solveit’s `/wsx` relay.
The relay forwards each frame unchanged to every other peer on that
channel. `solvecdp` uses
[`Channel`](https://AnswerDotAI.github.io/dialoghelper/core.html#channel)
to communicate with the
[solveit-chrome](https://github.com/AnswerDotAI/solveit-chrome)
extension.

[`Channel`](https://AnswerDotAI.github.io/dialoghelper/core.html#channel)
sends JSON dictionaries. Pass the fields as keyword arguments to `send`.
Incoming frames become `AttrDict` objects. If a frame’s `id` matches a
pending request, it completes that request. Other frames go into the
`events` queue.

Here we connect two peers and send a greeting. The second peer reads it
from `events`.

``` python
ch1,ch2 = await Channel.connect('test'),await Channel.connect('test')
await ch1.send(hello='world')
test_eq((await ch2.events.get()).hello, 'world')
```

`request` adds a unique `id` and waits for a reply with that `id`.
Requests can run concurrently. The peer answering a request must include
its `id` in the reply. This example replies with twice the supplied
number.

``` python
async def _responder():
    msg = await ch2.events.get()
    await ch2.send(id=msg.id, result=msg.x*2)

task = asyncio.create_task(_responder())
test_eq((await ch1.request(x=21)).result, 42)
```

Check `is_open` before and after closing the connections.

``` python
assert ch1.is_open
await ch1.close()
await ch2.close()
assert not ch1.is_open
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L340"
target="_blank" style="float:right; font-size:smaller">source</a>

### display_response

``` python
def display_response(
    display:str, result:str=None
):
```

*Return a special response where `display` is added as markdown/HTML to
the prompt output, and `result` is returned to the LLM*

[`realpath()`](https://AnswerDotAI.github.io/dialoghelper/core.html#realpath)
returns the gateway’s on-disk root. `realpath('')` returns the current
notebook’s folder. Other relative paths use that folder, regardless of
the kernel’s cwd. A leading slash uses the gateway root. The binding
follows notebook renames. Targets need not exist. `..` can move up
within the root. The gateway resolves symlinks within its root and
rejects dangling symlinks and paths escaping the root.

These helpers use `dh_settings['kid']`, initialized from
`RUSTYGATE_KERNEL_ID`. An unbound kernel can resolve root-qualified
paths but has no notebook-relative base.
[`list_dialogs()`](https://AnswerDotAI.github.io/dialoghelper/core.html#list_dialogs)
lists the current notebook’s folder by default and keeps the existing
notebook-extension and folder-suffix conventions.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L346"
target="_blank" style="float:right; font-size:smaller">source</a>

### realpath

``` python
async def realpath(
    subpath:str='/', # Gateway-root-relative with a leading slash, otherwise relative to the current notebook's folder
)->str:
```

*Get the on-disk path from the kernel’s current notebook binding.*

``` python
await realpath('data/results')
```

    '/Users/jhoward'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L353"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_dialogs

``` python
async def list_dialogs(
    subpath:str='', # Path under data root (absolute with `/`, else relative to current dialog's folder)
    depth:int=1, # Directory depth
)->dict:
```

*List dialogs and folders under `subpath`. Folders have `/` suffix.*

``` python
(await list_dialogs())['items'][-4:]
```

    ['06_utils.ipynb', '07_test.ipynb', 'data/', 'index.ipynb']

``` python
(await list_dialogs('/'))['items'][:5]
```

    ['Applications/', 'CRAFT.ipynb', 'CRAFTs/', 'Desktop/', 'Documents/']

## Dialog model

[`Dialog`](https://AnswerDotAI.github.io/dialoghelper/core.html#dialog)
and
[`Message`](https://AnswerDotAI.github.io/dialoghelper/core.html#message)
extend [aidialog](https://github.com/AnswerDotAI/aidialog)’s classes
with Solveit’s metadata fields. `meta_attrs` maps each attribute to its
stored metadata key. Reading a message makes these fields available as
attributes. Serializing the message writes them back to metadata.

At startup, a Solveit kernel registers this
[`Dialog`](https://AnswerDotAI.github.io/dialoghelper/core.html#dialog)
class with `aidialog.dlgskill.set_dlg(path, cls=Dialog)`. Subsequent
reads through `aidialog.dlgskill` use these subclasses for the current
dialog.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L381"
target="_blank" style="float:right; font-size:smaller">source</a>

### Dialog

``` python
def Dialog(
    messages:NoneType=None, name:str='', meta:NoneType=None
):
```

*aidialog
[`Dialog`](https://AnswerDotAI.github.io/dialoghelper/core.html#dialog)
producing dialoghelper
[`Message`](https://AnswerDotAI.github.io/dialoghelper/core.html#message)s*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L369"
target="_blank" style="float:right; font-size:smaller">source</a>

### Message

``` python
def Message(
    content:str='', # The message text: markdown, code, or a prompt's request
    output:str='', # Jupyter-style output list (code and prompt messages), or ''
    id:NoneType=None, # Message id; 4 random hex bytes if None
    msg_type:str='code', # One of `smsg_types`: code, note, prompt, or raw
    attachments:NoneType=None, # `Attachment`s carried by the message
    meta:NoneType=None, # Remaining cell metadata, carried verbatim through save/load
    **xtras
):
```

*aidialog
[`Message`](https://AnswerDotAI.github.io/dialoghelper/core.html#message)
with solveit’s persisted fields declared*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L401"
target="_blank" style="float:right; font-size:smaller">source</a>

### dlg_path

``` python
def dlg_path(
    dname:str=''
):
```

*The `.ipynb` file for `dname` (default: the current dialog)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L391"
target="_blank" style="float:right; font-size:smaller">source</a>

### data_root

``` python
def data_root():
```

*The data directory dialog names are relative to*

The editing tools use the gateway’s cells API. They send batches of
operations to add or delete cells, merge metadata, or toggle flags. An
edit that needs a message’s existing content reads that cell before
sending its replacement.

Writes do not check whether the cell changed since the read. The last
write to a field wins, as it did with direct file editing. Solveit
updates its view from the gateway’s broadcasts rather than waiting for a
file watcher to report a diff.

Gateway kernels have `RUSTYGATE_URL` in their environment. Set
`dh_settings['rusty']` to override it during development or tests.
[`cells_client`](https://AnswerDotAI.github.io/dialoghelper/core.html#cells_client)
binds a client to one dialog’s notebook under the gateway’s data root.
[`data_root`](https://AnswerDotAI.github.io/dialoghelper/core.html#data_root)
resolves that root for operations that still use filesystem paths.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L406"
target="_blank" style="float:right; font-size:smaller">source</a>

### cells_client

``` python
def cells_client(
    dname:str=''
):
```

*Create a cells client for the current kernel or a separately addressed
notebook*

## View/edit dialog

``` python
s = 'x = 21\nx*2\ndone'
test_eq(_lnhashs_content(s), '\n'.join(lnhash(i,l)+l for i,l in enumerate(s.splitlines(),1)))
test_eq(_lnhashs_content(s, 2, -1).splitlines()[0], lnhash(2,'x*2')+'x*2')
test_eq(len(_lnhashs_content(s, 1, 2).splitlines()), 2)
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L451"
target="_blank" style="float:right; font-size:smaller">source</a>

### read_msg

``` python
async def read_msg(
    n:int=-1, # Message index (if relative, +ve is downwards)
    relative:bool=True, # Is `n` relative to current message (True) or absolute (False)?
    id:str=None, # Message id to find (defaults to current message)
    start_line:int=1, # Starting line to view
    end_line:int=None, # End line (defaults to last line if None; -1 for EOF)
    nums:bool=False, # Whether to show line numbers
    lnhashs:bool=False, # Show exhash `lineno|hash|` addresses instead of line numbers?
    dname:str='', # Dialog to get info for; defaults to current dialog
)->dict:
```

*Get the message indexed in the current dialog.* NB: Messages in the
current dialog above the current message are *already* visible; use this
only when you need line numbers for editing operations, or for messages
not in the current dialog or below the current message. - To get the
exact message use `n=0` and `relative=True` together with `id`. - To get
a relative message use `n` (relative position index). - To get the nth
message use `n` with `relative=False`, e.g `n=0` first message, `n=-1`
last message. {dname}

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L513"
target="_blank" style="float:right; font-size:smaller">source</a>

### find_msgs

``` python
async def find_msgs(
    re_pattern:str='', # Optional regex to search for (re.DOTALL+re.MULTILINE is used)
    msg_type:str=None, # optional limit by message type ('code', 'note', or 'prompt')
    before:int=0, # Include additional n msgs before matches
    after:int=0, # Include additional n msgs after matches
    context:int=None, # Include additional n msgs around matches (default 1, or 0 when `headers_only`)
    use_case:bool=False, # Use case-sensitive matching?
    use_regex:bool=True, # Use regex matching?
    only_err:bool=None, # Only messages with errors if True, only those without if False, all if None
    only_exp:bool=None, # Only exported messages if True, only unexported if False, all if None
    ids:str='', # Optionally filter by comma-separated list of message ids
    limit:int=None, # Optionally limit number of returned items
    include_output:bool=True, # Include output in returned dict?
    include_meta:bool=True, # Include all additional message metadata
    as_xml:bool=False, # Use concise unescaped XML output format
    nums:bool=False, # Show line numbers?
    trunc_out:bool=False, # Middle-out truncate code output to 100 characters?
    trunc_in:bool=False, # Middle-out truncate cell content to 80 characters?
    headers_only:bool=False, # Only return note messages that are headers (first line only); cannot be used together with `header_section`
    header_section:str=None, # Find section starting with this header; returns it plus all children
    include_skipped:bool=False, # Include messages hidden from AI (skipped)?
    dname:str='', # Dialog to get info for; defaults to current dialog
)->list[dict]: # Messages in requested dialog that contain the given information
```

*Often it is more efficient to call
[`view_dlg`](https://AnswerDotAI.github.io/dialoghelper/core.html#view_dlg)
(not
[`find_msgs`](https://AnswerDotAI.github.io/dialoghelper/core.html#find_msgs))
to see the whole dialog, so you can use it all from then on.* {dname}
Message ids are identical to those in LLM chat history, so do NOT call
this to view a specific message if it’s in the chat history–instead use
[`view_msg`](https://AnswerDotAI.github.io/dialoghelper/core.html#view_msg).
Do NOT use find_msgs to view message content in the current dialog above
the current prompt – these are *already* provided in LLM context, so
just read the content there directly. (NB: LLM context only includes
messages *above* the current prompt, whereas
[`find_msgs`](https://AnswerDotAI.github.io/dialoghelper/core.html#find_msgs)
can access *all* messages.) To refer to a found message from code, use
its `id` field.

``` python
1+1
```

    2

``` python
r = await find_msgs(r'1\+1', include_meta=False, include_output=True, context=0)
r
```

    [{'id': '8ce548d6',
      'content': '1+1',
      'output': '2',
      'msg_type': 'code',
      'exported': False},
     {'id': '372c363d',
      'content': "_id = await add_msg('1+1', run=True, msg_type='code')",
      'output': '',
      'msg_type': 'code',
      'exported': False}]

``` python
hl_md(await find_msgs(r'1\+1', include_meta=False, as_xml=True, context=0))
```

``` html
<msgs><code id="8ce548d6">1+1<out>2</out></code><code id="372c363d">_id = await add_msg('1+1', run=True, msg_type='code')</code></msgs>
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L563"
target="_blank" style="float:right; font-size:smaller">source</a>

### view_dlg

``` python
async def view_dlg(
    dname:str='', # Dialog to get info for; defaults to current dialog
    msg_type:str=None, # optional limit by message type ('code', 'note', or 'prompt')
    nums:bool=False, # Whether to show line numbers
    include_output:bool=False, # Include output in returned dict?
    trunc_out:bool=True, # Middle-out truncate code output to 100 characters (only applies if `include_output`)?
    trunc_in:bool=False, # Middle-out truncate cell content to 80 characters?
    include_skipped:bool=False, # Include messages hidden from AI (skipped)?
)->str:
```

*Concise XML view of all messages (optionally filtered by type), not
including metadata. Often it is more efficient to call this to see the
whole dialog at once (including line numbers if needed), instead of
running
[`find_msgs`](https://AnswerDotAI.github.io/dialoghelper/core.html#find_msgs)
or
[`view_msg`](https://AnswerDotAI.github.io/dialoghelper/core.html#view_msg)
multiple times.*

``` python
hl_md((await view_dlg(nums=True))[:500])
```

``` html
<msgs><code id="955b9784">     1 │ #| default_exp core</code><code id="a982e24d">     1 │ from dialoghelper import *</code><markdown id="0aafe008">     1 │ # dialoghelper
     2 │ 
     3 │ > Inspect and edit Solveit dialogs, run messages, manage kernels, and communicate with browser pages
     4 │ 
     5 │ Use these helpers from a Solveit kernel. Most examples below need a running Solveit server and a current dialog. They change that dialog and include examples that run prompts or restart and 
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L620"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_msg

``` python
async def add_msg(
    content:str, # Content of the message (i.e the message prompt, code, or note text)
    msg_type:str='note', # Message type, can be 'code', 'note', or 'prompt'
    run:bool=False, # Run the message?
    *,
    placement:str='', # Location to place message. Can be 'at_start' or 'at_end', and if id provided or in curr dlg can also be 'add_after' or 'add_before'. Defaults to 'at_end' if no id and not targeting curr dlg
    id:str=None, # id of message that placement is relative to (if None, uses current message)
    dname:str='', # Dialog to add to; defaults to current dialog (`run` only has an effect if dialog is currently running)
    output:str='', # Prompt/code output; Code outputs must be .ipynb-compatible JSON array
    exported:int | None=0, # Mark message as exported (stored as nbdev `export` metadata)?
    skipped:int | None=0, # Hide message from prompt?
    i_collapsed:int | None=0, # Collapse input?
    o_collapsed:int | None=0, # Collapse output?
    heading_collapsed:int | None=0, # Collapse heading section?
    pinned:int | None=0, # Pin to context?
)->str: # Message ID of newly created message
```

*Add/update a message to the queue to show after code execution
completes, and optionally run it.* Code messages are run using python’s
restricted sandbox. **NB**: when creating multiple messages in a row,
after the 1st message set `id` to the result of the last
[`add_msg`](https://AnswerDotAI.github.io/dialoghelper/core.html#add_msg)
call, otherwise messages will appear in the dialog in REVERSE order.
{dname}

``` python
_id = await add_msg('testing')
_id
```

    'ee150d1b'

    '321f8ade'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L635"
target="_blank" style="float:right; font-size:smaller">source</a>

### read_msgid

``` python
async def read_msgid(
    id:str, # Message id to find
    start_line:int=1, # Starting line to view
    end_line:int=None, # End line (defaults to last line if None; -1 for EOF)
    nums:bool=False, # Whether to show line numbers
    lnhashs:bool=False, # Show exhash `lineno|hash|` addresses instead of line numbers?
    dname:str='', # Dialog to get message from; defaults to current dialog
    add_to_dlg:bool=False, # Whether to add message content to current dialog (as a raw message)
)->dict:
```

*Get message `id`. Message IDs can be view directly in LLM chat
history/context, or found in
[`find_msgs`](https://AnswerDotAI.github.io/dialoghelper/core.html#find_msgs)
results.* Use `add_to_dlg` if the LLM or human may need to refer to the
message content again later.

``` python
r = await read_msg(-2)
print((await read_msg(-2)).content)
```

    AttributeError: 'dict' object has no attribute 'content'
    [31m---------------------------------------------------------------------------[39m
    [31mAttributeError[39m                            Traceback (most recent call last)
    [36mCell[39m[36m [39m[32mIn[133][39m[32m, line 2[39m
    [32m      1[39m r = [38;5;28;01mawait[39;00m read_msg(-[32m2[39m)
    [32m----> [39m[32m2[39m print(([38;5;28;01mawait[39;00m read_msg(-[32m2[39m)).content)

    [31mAttributeError[39m: 'dict' object has no attribute 'content'

[`read_msg`](https://AnswerDotAI.github.io/dialoghelper/core.html#read_msg)
returns a `dict2obj` result. Access its fields as dictionary keys, such
as `r['content']`, or attributes, such as `r.content`. Endpoint helpers
also provide attribute access for JSON object responses.

``` python
bmsg = await add_msg('at bottom', placement='at_end')
```

``` python
test_eq((await read_msg(-1, relative=False))['id'], bmsg)  # at_end put it last
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L651"
target="_blank" style="float:right; font-size:smaller">source</a>

### view_msg

``` python
async def view_msg(
    id:str, # Message id to view
    dname:str='', # Dialog to get message from; defaults to current dialog
    nums:bool=True, # Whether to show line numbers
    lnhashs:bool=False, # Show exhash `lineno|hash|` addresses instead of line numbers?
    start_line:int=1, # Starting line to view. Rarely needed--read whole message in nearly all cases instead
    end_line:int=None, # End line (defaults to last line if None; -1 for EOF)
    incl_out:bool=False, # Append the message's output in an `<out>` block?
    trunc_out:bool=True, # Truncate an included output to ~512 chars?
    add_to_dlg:bool=False, # Whether to add message content to current dialog (as a raw message)
)->str:
```

*Views the* content\* of message `id`. Same as
`read_msgid(...)['content']`, defaulting to `nums=True`.\* Use
`add_to_dlg` if the LLM or human may need to refer to the message
content again later.

``` python
print((await view_msg(r.id)))
```

         1 │ testing

``` python
# dh_settings['dname'] = 'tmp'
# _id = await add_msg('testing', placement='at_end')
# print(_id)
# del(dh_settings['dname'])
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L671"
target="_blank" style="float:right; font-size:smaller">source</a>

### msg_ref

``` python
def msg_ref(
    id, dname:NoneType=None
):
```

*Markdown ref to a message — same-dialog `#_id` or cross-dialog
`#dname/_id` (anchors target DOM ids, which carry a `_` prefix)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L678"
target="_blank" style="float:right; font-size:smaller">source</a>

### del_msgs

``` python
async def del_msgs(
    ids:str=None, # Comma-separated ids of message(s) to delete
    dname:str='', # Dialog to get info for; defaults to current dialog
    log_changed:bool=False, # Add a note showing the deleted content?
)->list:
```

*Delete exactly the named messages (a collapsed heading’s hidden section
stays). DO NOT USE THIS unless you have been explicitly instructed to
delete messages.*

``` python
await del_msgs(bmsg)
await del_msgs(_id)
```

    ['ee150d1b']

    ['4bd0fdaf']

``` python
_id = await add_msg('1+1', run=True, msg_type='code')
```

``` python
await del_msgs(_id)
```

    ['b001fd04']

``` python
_id = await add_msg('Hi', run=True, msg_type='prompt')
```

``` python
await del_msgs(_id)
```

    ['4561db1c']

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L695"
target="_blank" style="float:right; font-size:smaller">source</a>

### run_and_prompt

``` python
async def run_and_prompt(
    code:str, # Python code to run
    prompt:str='Continue.', # Prompt to add after code execution
)->str:
```

*Run `code` and then run `prompt`, returning the resulting message ID.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L721"
target="_blank" style="float:right; font-size:smaller">source</a>

### update_msg

``` python
async def update_msg(
    id:str=None, # id of message to update (if None, uses current message)
    msg:Optional[Dict]=None, # Dictionary of field keys/values to update
    dname:str='', # Dialog to get info for; defaults to current dialog
    log_changed:bool=False, # Add a note showing the diff?
    *, content:str | None=None, # Content of the message (i.e the message prompt, code, or note text)
    msg_type:str | None=None, # Message type, can be 'code', 'note', or 'prompt'
    output:str | None=None, # Prompt/code output; Code outputs must be .ipynb-compatible JSON array
    exported:int | None=None, # Set export state (nbdev `export` metadata)?
    skipped:int | None=None, # Hide message from prompt?
    i_collapsed:int | None=None, # Collapse input?
    o_collapsed:int | None=None, # Collapse output?
    heading_collapsed:int | None=None, # Collapse heading section?
    pinned:int | None=None, # Pin to context?
    meta:dict | None=None, # Replace message meta wholesale
    mergemeta:dict | None=None, # Deep-merge into message meta; a `None` value deletes its key
)->str:
```

*Update an existing message. Provide either `msg` OR field key/values to
update.* - Use `content` param to update contents. - Only include
parameters to update–missing ones will be left unchanged. {dname}

``` python
_id = await add_msg('testing')
```

``` python
_id = await update_msg(_id, content='toasting')
```

``` python
_id = await update_msg(_id, skipped=1)
```

[`update_msg`](https://AnswerDotAI.github.io/dialoghelper/core.html#update_msg)
follows `Message.update` for metadata. `meta=` replaces the metadata
dictionary. `mergemeta=` merges nested dictionaries and deletes keys
whose value is `None`.

A metadata merge on its own uses a gateway `merge` operation. It follows
JSON merge-patch rules, where `null` deletes a key. A metadata
replacement, or a merge combined with content changes, uses the
serialized message in an unconditional `update` operation.

``` python
await update_msg(_id, mergemeta=dict(pins=dict(a=1)))
test_eq((await read_msgid(_id))['meta']['pins'], {'a': 1})
await update_msg(_id, mergemeta=dict(pins=None))
assert 'pins' not in (await read_msgid(_id))['meta']
```

``` python
msg = await read_msgid(_id)
msg['content'] = 'toasted'
await update_msg(msg=msg)
```

    'd8addd7b'

``` python
await del_msgs(_id)
```

    KeyboardInterrupt: 
    [31m---------------------------------------------------------------------------[39m
    [31mCancelledError[39m                            Traceback (most recent call last)
    [36mCell[39m[36m [39m[32mIn[177][39m[32m, line 1[39m
    [32m----> [39m[32m1[39m [38;5;28;01mawait[39;00m del_msgs(_id)

    [36mCell[39m[36m [39m[32mIn[149][39m[32m, line 14[39m, in [36mdel_msgs[39m[34m(ids, dname, log_changed)[39m
    [32m     10[39m     logs = []
    [32m     11[39m     [38;5;28;01mif[39;00m log_changed:
    [32m     12[39m         by = {c[[33m'id'[39m]: c [38;5;28;01mfor[39;00m c [38;5;28;01min[39;00m [38;5;28;01mawait[39;00m fc.cells(ids=ids)}
    [32m     13[39m         logs = [f"> Deleted {msg_ref(i, dname)}\n\n```\n{_cell2dict(by[i])[[33m'content'[39m]}\n```" [38;5;28;01mfor[39;00m i [38;5;28;01min[39;00m ids [38;5;28;01mif[39;00m i [38;5;28;01min[39;00m by]
    [32m---> [39m[32m14[39m     [38;5;28;01mawait[39;00m fc.apply([dict(op=[33m'delete'[39m, id=i) [38;5;28;01mfor[39;00m i [38;5;28;01min[39;00m ids])
    [32m     15[39m     [38;5;28;01mfor[39;00m l [38;5;28;01min[39;00m logs: [38;5;28;01mawait[39;00m add_msg(l)
    [32m     16[39m     [38;5;28;01mreturn[39;00m ids

    [36mFile [39m[32m~/aai-ws/jupyasyncclient/jupyasyncclient/files.py:166[39m, in [36mapply[39m[34m(self, ops)[39m
    [32m    163[39m [38;5;129m@patch[39m
    [32m    164[39m [38;5;28;01masync[39;00m [38;5;28;01mdef[39;00m[38;5;250m [39m[34mapply[39m([38;5;28mself[39m:JupyAsyncCellsClient, ops):
    [32m    165[39m     [33m"[39m[33mApply `ops` atomically, returning ids of added cells.[39m[33m"[39m
    [32m--> [39m[32m166[39m     m = [38;5;28;01mawait[39;00m [38;5;28mself[39m._op([38;5;28mself[39m.api.cells.post_cells, path=[38;5;28mself[39m.path, ops=ops)
    [32m    167[39m     [38;5;28mself[39m.hash = m[[33m'[39m[33mhash[39m[33m'[39m]
    [32m    168[39m     [38;5;28;01mreturn[39;00m m[[33m'[39m[33madded_ids[39m[33m'[39m]

    [36mFile [39m[32m~/aai-ws/jupyasyncclient/jupyasyncclient/files.py:37[39m, in [36m_op[39m[34m(self, op, **kw)[39m
    [32m     35[39m [33m"[39m[33mCall spec op `op` with `session_id` attached and None values dropped; a conditional-write 409 raises `HashMismatch`.[39m[33m"[39m
    [32m     36[39m kw = {k:v [38;5;28;01mfor[39;00m k,v [38;5;129;01min[39;00m [38;5;28mdict[39m(kw, session_id=[38;5;28mself[39m.session_id).items() [38;5;28;01mif[39;00m v [38;5;129;01mis[39;00m [38;5;129;01mnot[39;00m [38;5;28;01mNone[39;00m}
    [32m---> [39m[32m37[39m [38;5;28;01mtry[39;00m: [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m op(**kw)
    [32m     38[39m [38;5;28;01mexcept[39;00m APIError [38;5;28;01mas[39;00m e:
    [32m     39[39m     [38;5;28;01mif[39;00m e.status_code==[32m409[39m [38;5;129;01mand[39;00m [38;5;28misinstance[39m(e.raw, [38;5;28mdict[39m) [38;5;129;01mand[39;00m [33m'[39m[33mhash[39m[33m'[39m [38;5;129;01min[39;00m e.raw: [38;5;28;01mraise[39;00m HashMismatch(e.raw[[33m'[39m[33mhash[39m[33m'[39m]) [38;5;28;01mfrom[39;00m[38;5;250m [39m[34;01me[39;00m

    [36mFile [39m[32m~/aai-ws/fastspec/fastspec/oapi.py:148[39m, in [36m__call__[39m[34m(self, *args, **kwargs)[39m
    [32m    146[39m stream, url, headers, query, route, kw = [38;5;28mself[39m._prep(args, kwargs)
    [32m    147[39m [38;5;28;01mif[39;00m stream: [38;5;28;01mreturn[39;00m [38;5;28mself[39m._stream(url, headers=headers, query=query, route=route, **kw)
    [32m--> [39m[32m148[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._request(url, headers=headers, query=query, route=route, **kw)

    [36mFile [39m[32m~/aai-ws/fastspec/fastspec/oapi.py:120[39m, in [36m_request[39m[34m(self, url, headers, query, body, route, **kwargs)[39m
    [32m    116[39m [38;5;129m@patch[39m
    [32m    117[39m [38;5;129m@delegates[39m(AsyncTransport.request) [38;5;66;03m# files, raw[39;00m
    [32m    118[39m [38;5;28;01masync[39;00m [38;5;28;01mdef[39;00m[38;5;250m [39m[34m_request[39m([38;5;28mself[39m:OpFunc, url, *, headers=[38;5;28;01mNone[39;00m, query=[38;5;28;01mNone[39;00m, body=[38;5;28;01mNone[39;00m, route=[38;5;28;01mNone[39;00m, **kwargs):
    [32m    119[39m     [33m"[39m[33mExecute an HTTP request and return decoded response.[39m[33m"[39m
    [32m--> [39m[32m120[39m     [38;5;28;01mtry[39;00m: [38;5;28;01mreturn[39;00m dict2obj([38;5;28;01mawait[39;00m [38;5;28mself[39m.client.request([38;5;28mself[39m.verb, url, headers=headers, params=query, json=body, **kwargs))
    [32m    121[39m     [38;5;28;01mexcept[39;00m [38;5;167;01mException[39;00m [38;5;28;01mas[39;00m e: [38;5;28mself[39m._raise_with_context(e, endpoint=[33m'[39m[33m'[39m, route=route, query=query, body=body)

    [36mFile [39m[32m~/aai-ws/fasttransport/fasttransport/core.py:58[39m, in [36mAsyncTransport.request[39m[34m(self, method, url, headers, params, json, data, files, content, raw)[39m
    [32m     56[39m [33m"[39m[33mExecute a request and decode JSON/text/binary response.[39m[33m"[39m
    [32m     57[39m [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m [38;5;28mself[39m._client() [38;5;28;01mas[39;00m client:
    [32m---> [39m[32m58[39m     resp = [38;5;28;01mawait[39;00m client.request(method, url, headers=[38;5;28mself[39m._request_headers(headers, files=files),
    [32m     59[39m         params=params, json=json, data=data, files=files, content=content)
    [32m     60[39m     [38;5;28;01mtry[39;00m: resp.raise_for_status()
    [32m     61[39m     [38;5;28;01mexcept[39;00m httpx2.HTTPStatusError [38;5;28;01mas[39;00m e:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1635[39m, in [36mAsyncClient.request[39m[34m(self, method, url, content, data, files, json, params, headers, cookies, auth, follow_redirects, timeout, extensions)[39m
    [32m   1620[39m     warnings.warn(message, [38;5;167;01mDeprecationWarning[39;00m, stacklevel=[32m2[39m)
    [32m   1622[39m request = [38;5;28mself[39m.build_request(
    [32m   1623[39m     method=method,
    [32m   1624[39m     url=url,
    [32m   (...)[39m[32m   1633[39m     extensions=extensions,
    [32m   1634[39m )
    [32m-> [39m[32m1635[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m.send(request, auth=auth, follow_redirects=follow_redirects)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1818[39m, in [36mAsyncClient.send[39m[34m(self, request, stream, auth, follow_redirects)[39m
    [32m   1814[39m [38;5;28mself[39m._set_timeout(request)
    [32m   1816[39m auth = [38;5;28mself[39m._build_request_auth(request, auth)
    [32m-> [39m[32m1818[39m response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_handling_auth(
    [32m   1819[39m     request,
    [32m   1820[39m     auth=auth,
    [32m   1821[39m     follow_redirects=follow_redirects,
    [32m   1822[39m     history=[],
    [32m   1823[39m )
    [32m   1824[39m [38;5;28;01mtry[39;00m:
    [32m   1825[39m     [38;5;28;01mif[39;00m [38;5;129;01mnot[39;00m stream:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1846[39m, in [36mAsyncClient._send_handling_auth[39m[34m(self, request, auth, follow_redirects, history)[39m
    [32m   1843[39m request = [38;5;28;01mawait[39;00m auth_flow.[34m__anext__[39m()
    [32m   1845[39m [38;5;28;01mwhile[39;00m [38;5;28;01mTrue[39;00m:
    [32m-> [39m[32m1846[39m     response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_handling_redirects(
    [32m   1847[39m         request,
    [32m   1848[39m         follow_redirects=follow_redirects,
    [32m   1849[39m         history=history,
    [32m   1850[39m     )
    [32m   1851[39m     [38;5;28;01mtry[39;00m:
    [32m   1852[39m         [38;5;28;01mtry[39;00m:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1881[39m, in [36mAsyncClient._send_handling_redirects[39m[34m(self, request, follow_redirects, history)[39m
    [32m   1878[39m [38;5;28;01mfor[39;00m hook [38;5;129;01min[39;00m [38;5;28mself[39m._event_hooks[[33m"[39m[33mrequest[39m[33m"[39m]:
    [32m   1879[39m     [38;5;28;01mawait[39;00m hook(request)
    [32m-> [39m[32m1881[39m response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_single_request(request)
    [32m   1882[39m [38;5;28;01mtry[39;00m:
    [32m   1883[39m     [38;5;28;01mfor[39;00m hook [38;5;129;01min[39;00m [38;5;28mself[39m._event_hooks[[33m"[39m[33mresponse[39m[33m"[39m]:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1915[39m, in [36mAsyncClient._send_single_request[39m[34m(self, request)[39m
    [32m   1912[39m     [38;5;28;01mraise[39;00m [38;5;167;01mRuntimeError[39;00m([33m"[39m[33mAttempted to send a sync request with an AsyncClient instance.[39m[33m"[39m)
    [32m   1914[39m [38;5;28;01mwith[39;00m request_context(request=request):
    [32m-> [39m[32m1915[39m     response = [38;5;28;01mawait[39;00m transport.handle_async_request(request)
    [32m   1917[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(response.stream, AsyncByteStream)
    [32m   1918[39m response.request = request

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_transports/default.py:388[39m, in [36mAsyncHTTPTransport.handle_async_request[39m[34m(self, request)[39m
    [32m    375[39m req = httpcore2.Request(
    [32m    376[39m     method=request.method,
    [32m    377[39m     url=httpcore2.URL(
    [32m   (...)[39m[32m    385[39m     extensions=request.extensions,
    [32m    386[39m )
    [32m    387[39m [38;5;28;01mwith[39;00m map_httpcore_exceptions():
    [32m--> [39m[32m388[39m     resp = [38;5;28;01mawait[39;00m [38;5;28mself[39m._pool.handle_async_request(req)
    [32m    390[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(resp.stream, typing.AsyncIterable)
    [32m    392[39m [38;5;28;01mreturn[39;00m Response(
    [32m    393[39m     status_code=resp.status,
    [32m    394[39m     headers=resp.headers,
    [32m    395[39m     stream=AsyncResponseStream(resp.stream),
    [32m    396[39m     extensions=resp.extensions,
    [32m    397[39m )

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection_pool.py:242[39m, in [36mAsyncConnectionPool.handle_async_request[39m[34m(self, request)[39m
    [32m    239[39m         closing = [38;5;28mself[39m._assign_requests_to_connections()
    [32m    241[39m     [38;5;28;01mawait[39;00m [38;5;28mself[39m._close_connections(closing)
    [32m--> [39m[32m242[39m     [38;5;28;01mraise[39;00m exc [38;5;28;01mfrom[39;00m[38;5;250m [39m[38;5;28;01mNone[39;00m
    [32m    244[39m [38;5;66;03m# Return the response. Note that in this case we still have to manage[39;00m
    [32m    245[39m [38;5;66;03m# the point at which the response is closed.[39;00m
    [32m    246[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(response.stream, typing.AsyncIterable)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection_pool.py:224[39m, in [36mAsyncConnectionPool.handle_async_request[39m[34m(self, request)[39m
    [32m    220[39m connection = [38;5;28;01mawait[39;00m pool_request.wait_for_connection(timeout=timeout)
    [32m    222[39m [38;5;28;01mtry[39;00m:
    [32m    223[39m     [38;5;66;03m# Send the request on the assigned connection.[39;00m
    [32m--> [39m[32m224[39m     response = [38;5;28;01mawait[39;00m connection.handle_async_request(pool_request.request)
    [32m    225[39m [38;5;28;01mexcept[39;00m ConnectionNotAvailable:
    [32m    226[39m     [38;5;66;03m# In some cases a connection may initially be available to[39;00m
    [32m    227[39m     [38;5;66;03m# handle a request, but then become unavailable.[39;00m
    [32m    228[39m     [38;5;66;03m#[39;00m
    [32m    229[39m     [38;5;66;03m# In this case we clear the connection and try again.[39;00m
    [32m    230[39m     pool_request.clear_connection()

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection.py:96[39m, in [36mAsyncHTTPConnection.handle_async_request[39m[34m(self, request)[39m
    [32m     93[39m     [38;5;28mself[39m._connect_failed = [38;5;28;01mTrue[39;00m
    [32m     94[39m     [38;5;28;01mraise[39;00m exc
    [32m---> [39m[32m96[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._connection.handle_async_request(request)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:125[39m, in [36mAsyncHTTP11Connection.handle_async_request[39m[34m(self, request)[39m
    [32m    123[39m     [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m Trace([33m"[39m[33mresponse_closed[39m[33m"[39m, logger, request) [38;5;28;01mas[39;00m trace:
    [32m    124[39m         [38;5;28;01mawait[39;00m [38;5;28mself[39m._response_closed()
    [32m--> [39m[32m125[39m [38;5;28;01mraise[39;00m exc

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:97[39m, in [36mAsyncHTTP11Connection.handle_async_request[39m[34m(self, request)[39m
    [32m     88[39m     [38;5;28;01mpass[39;00m
    [32m     90[39m [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m Trace([33m"[39m[33mreceive_response_headers[39m[33m"[39m, logger, request, kwargs) [38;5;28;01mas[39;00m trace:
    [32m     91[39m     (
    [32m     92[39m         http_version,
    [32m     93[39m         status,
    [32m     94[39m         reason_phrase,
    [32m     95[39m         headers,
    [32m     96[39m         trailing_data,
    [32m---> [39m[32m97[39m     ) = [38;5;28;01mawait[39;00m [38;5;28mself[39m._receive_response_headers(**kwargs)
    [32m     98[39m     trace.return_value = (
    [32m     99[39m         http_version,
    [32m    100[39m         status,
    [32m    101[39m         reason_phrase,
    [32m    102[39m         headers,
    [32m    103[39m     )
    [32m    105[39m network_stream = [38;5;28mself[39m._network_stream

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:167[39m, in [36mAsyncHTTP11Connection._receive_response_headers[39m[34m(self, request)[39m
    [32m    164[39m timeout = timeouts.get([33m"[39m[33mread[39m[33m"[39m, [38;5;28;01mNone[39;00m)
    [32m    166[39m [38;5;28;01mwhile[39;00m [38;5;28;01mTrue[39;00m:
    [32m--> [39m[32m167[39m     event = [38;5;28;01mawait[39;00m [38;5;28mself[39m._receive_event(timeout=timeout)
    [32m    168[39m     [38;5;28;01mif[39;00m [38;5;28misinstance[39m(event, h11.Response):
    [32m    169[39m         [38;5;28;01mbreak[39;00m

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:200[39m, in [36mAsyncHTTP11Connection._receive_event[39m[34m(self, timeout)[39m
    [32m    197[39m     event = [38;5;28mself[39m._h11_state.next_event()
    [32m    199[39m [38;5;28;01mif[39;00m event [38;5;129;01mis[39;00m h11.NEED_DATA:
    [32m--> [39m[32m200[39m     data = [38;5;28;01mawait[39;00m [38;5;28mself[39m._network_stream.read([38;5;28mself[39m.READ_NUM_BYTES, timeout=timeout)
    [32m    202[39m     [38;5;66;03m# If we feed this case through h11 we'll raise an exception like:[39;00m
    [32m    203[39m     [38;5;66;03m#[39;00m
    [32m    204[39m     [38;5;66;03m#     httpcore2.RemoteProtocolError: can't handle event type[39;00m
    [32m   (...)[39m[32m    208[39m     [38;5;66;03m# perspective. Instead we handle this case distinctly and treat[39;00m
    [32m    209[39m     [38;5;66;03m# it as a ConnectError.[39;00m
    [32m    210[39m     [38;5;28;01mif[39;00m data == [33mb[39m[33m"[39m[33m"[39m [38;5;129;01mand[39;00m [38;5;28mself[39m._h11_state.their_state == h11.SEND_RESPONSE:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_backends/anyio.py:37[39m, in [36mAnyIOStream.read[39m[34m(self, max_bytes, timeout)[39m
    [32m     35[39m [38;5;28;01mwith[39;00m anyio.fail_after(timeout):
    [32m     36[39m     [38;5;28;01mtry[39;00m:
    [32m---> [39m[32m37[39m         [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._stream.receive(max_bytes=max_bytes)
    [32m     38[39m     [38;5;28;01mexcept[39;00m anyio.EndOfStream:  [38;5;66;03m# pragma: no cover[39;00m
    [32m     39[39m         [38;5;28;01mreturn[39;00m [33mb[39m[33m"[39m[33m"[39m

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/anyio/_backends/_asyncio.py:1337[39m, in [36mSocketStream.receive[39m[34m(self, max_bytes)[39m
    [32m   1331[39m [38;5;28;01mif[39;00m (
    [32m   1332[39m     [38;5;129;01mnot[39;00m [38;5;28mself[39m._protocol.read_event.is_set()
    [32m   1333[39m     [38;5;129;01mand[39;00m [38;5;129;01mnot[39;00m [38;5;28mself[39m._transport.is_closing()
    [32m   1334[39m     [38;5;129;01mand[39;00m [38;5;129;01mnot[39;00m [38;5;28mself[39m._protocol.is_at_eof
    [32m   1335[39m ):
    [32m   1336[39m     [38;5;28mself[39m._transport.resume_reading()
    [32m-> [39m[32m1337[39m     [38;5;28;01mawait[39;00m [38;5;28mself[39m._protocol.read_event.wait()
    [32m   1338[39m     [38;5;28mself[39m._transport.pause_reading()
    [32m   1339[39m [38;5;28;01melse[39;00m:

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/asyncio/locks.py:213[39m, in [36mEvent.wait[39m[34m(self)[39m
    [32m    211[39m [38;5;28mself[39m._waiters.append(fut)
    [32m    212[39m [38;5;28;01mtry[39;00m:
    [32m--> [39m[32m213[39m     [38;5;28;01mawait[39;00m fut
    [32m    214[39m     [38;5;28;01mreturn[39;00m [38;5;28;01mTrue[39;00m
    [32m    215[39m [38;5;28;01mfinally[39;00m:

    [31mCancelledError[39m: 

``` python
_edit_id = await add_msg('This message should be found.\n\nThis is a multiline message.')
_edit_id
```

    'ca8a365a'

This message should be found.

This is a multiline message.

This message should be found.

This is a multiline message.

This message should be found.

This is a multiline message.

This message should be found.

This is a multiline message.

``` python
print((await read_msg())['content'])
```

    KeyboardInterrupt: 
    [31m---------------------------------------------------------------------------[39m
    [31mCancelledError[39m                            Traceback (most recent call last)
    [36mCell[39m[36m [39m[32mIn[181][39m[32m, line 1[39m
    [32m----> [39m[32m1[39m print(([38;5;28;01mawait[39;00m read_msg())[[33m'content'[39m])

    [36mCell[39m[36m [39m[32mIn[107][39m[32m, line 24[39m, in [36mread_msg[39m[34m(n, relative, id, start_line, end_line, nums, lnhashs, dname)[39m
    [32m     20[39m     fc = cells_client(dname)
    [32m     21[39m     [38;5;28;01mif[39;00m relative:
    [32m     22[39m         [38;5;28;01mif[39;00m [38;5;28;01mnot[39;00m id: id = aidialog.dlgskill.cur_msgid
    [32m     23[39m         [38;5;28;01mif[39;00m [38;5;28;01mnot[39;00m id: [38;5;28;01mreturn[39;00m {[33m'msg'[39m:[38;5;28;01mNone[39;00m}
    [32m---> [39m[32m24[39m         cells = [38;5;28;01mawait[39;00m fc.cells(ids=id, context=abs(n) [38;5;28;01mor[39;00m [38;5;28;01mNone[39;00m)
    [32m     25[39m         anchor = first(i [38;5;28;01mfor[39;00m i,c [38;5;28;01min[39;00m enumerate(cells) [38;5;28;01mif[39;00m c[[33m'id'[39m]==id)
    [32m     26[39m         [38;5;28;01mif[39;00m anchor [38;5;28;01mis[39;00m [38;5;28;01mNone[39;00m: [38;5;28;01mreturn[39;00m {[33m'msg'[39m:[38;5;28;01mNone[39;00m}
    [32m     27[39m         [38;5;28;01mif[39;00m [38;5;28;01mnot[39;00m [32m0[39m<=anchor+n<len(cells): [38;5;28;01mreturn[39;00m {[33m'msg'[39m:[38;5;28;01mNone[39;00m}

    [36mFile [39m[32m~/aai-ws/jupyasyncclient/jupyasyncclient/files.py:147[39m, in [36mcells[39m[34m(self, ids, idx, q, cell_type, meta, meta_not, limit, context, fields)[39m
    [32m    134[39m [38;5;129m@patch[39m
    [32m    135[39m [38;5;28;01masync[39;00m [38;5;28;01mdef[39;00m[38;5;250m [39m[34mcells[39m([38;5;28mself[39m:JupyAsyncCellsClient,
    [32m    136[39m     ids=[38;5;28;01mNone[39;00m, [38;5;66;03m# Cell ids to keep: comma-separated str, or a list[39;00m
    [32m   (...)[39m[32m    144[39m     fields=[38;5;28;01mNone[39;00m, [38;5;66;03m# Comma-separated extras: 'hashes', 'meta', 'attachments'[39;00m
    [32m    145[39m ):
    [32m    146[39m     [33m"[39m[33mThe notebook[39m[33m'[39m[33ms cells in document order, optionally selected and filtered (the gateway[39m[33m'[39m[33ms cells GET stages).[39m[33m"[39m
    [32m--> [39m[32m147[39m     m = [38;5;28;01mawait[39;00m [38;5;28mself[39m._op([38;5;28mself[39m.api.cells.get_cells, path=[38;5;28mself[39m.path, ids=_cs(ids), idx=_cs(idx), q=q, cell_type=cell_type,
    [32m    148[39m         meta=[38;5;28;01mNone[39;00m [38;5;28;01mif[39;00m meta [38;5;129;01mis[39;00m [38;5;28;01mNone[39;00m [38;5;28;01melse[39;00m json.dumps(meta), meta_not=[38;5;28;01mNone[39;00m [38;5;28;01mif[39;00m meta_not [38;5;129;01mis[39;00m [38;5;28;01mNone[39;00m [38;5;28;01melse[39;00m json.dumps(meta_not),
    [32m    149[39m         limit=limit, context=context, fields=fields)
    [32m    150[39m     [38;5;28mself[39m.hash = m[[33m'[39m[33mhash[39m[33m'[39m]
    [32m    151[39m     [38;5;28mself[39m.matched = m.get([33m'[39m[33mmatched[39m[33m'[39m)

    [36mFile [39m[32m~/aai-ws/jupyasyncclient/jupyasyncclient/files.py:37[39m, in [36m_op[39m[34m(self, op, **kw)[39m
    [32m     35[39m [33m"[39m[33mCall spec op `op` with `session_id` attached and None values dropped; a conditional-write 409 raises `HashMismatch`.[39m[33m"[39m
    [32m     36[39m kw = {k:v [38;5;28;01mfor[39;00m k,v [38;5;129;01min[39;00m [38;5;28mdict[39m(kw, session_id=[38;5;28mself[39m.session_id).items() [38;5;28;01mif[39;00m v [38;5;129;01mis[39;00m [38;5;129;01mnot[39;00m [38;5;28;01mNone[39;00m}
    [32m---> [39m[32m37[39m [38;5;28;01mtry[39;00m: [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m op(**kw)
    [32m     38[39m [38;5;28;01mexcept[39;00m APIError [38;5;28;01mas[39;00m e:
    [32m     39[39m     [38;5;28;01mif[39;00m e.status_code==[32m409[39m [38;5;129;01mand[39;00m [38;5;28misinstance[39m(e.raw, [38;5;28mdict[39m) [38;5;129;01mand[39;00m [33m'[39m[33mhash[39m[33m'[39m [38;5;129;01min[39;00m e.raw: [38;5;28;01mraise[39;00m HashMismatch(e.raw[[33m'[39m[33mhash[39m[33m'[39m]) [38;5;28;01mfrom[39;00m[38;5;250m [39m[34;01me[39;00m

    [36mFile [39m[32m~/aai-ws/fastspec/fastspec/oapi.py:148[39m, in [36m__call__[39m[34m(self, *args, **kwargs)[39m
    [32m    146[39m stream, url, headers, query, route, kw = [38;5;28mself[39m._prep(args, kwargs)
    [32m    147[39m [38;5;28;01mif[39;00m stream: [38;5;28;01mreturn[39;00m [38;5;28mself[39m._stream(url, headers=headers, query=query, route=route, **kw)
    [32m--> [39m[32m148[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._request(url, headers=headers, query=query, route=route, **kw)

    [36mFile [39m[32m~/aai-ws/fastspec/fastspec/oapi.py:120[39m, in [36m_request[39m[34m(self, url, headers, query, body, route, **kwargs)[39m
    [32m    116[39m [38;5;129m@patch[39m
    [32m    117[39m [38;5;129m@delegates[39m(AsyncTransport.request) [38;5;66;03m# files, raw[39;00m
    [32m    118[39m [38;5;28;01masync[39;00m [38;5;28;01mdef[39;00m[38;5;250m [39m[34m_request[39m([38;5;28mself[39m:OpFunc, url, *, headers=[38;5;28;01mNone[39;00m, query=[38;5;28;01mNone[39;00m, body=[38;5;28;01mNone[39;00m, route=[38;5;28;01mNone[39;00m, **kwargs):
    [32m    119[39m     [33m"[39m[33mExecute an HTTP request and return decoded response.[39m[33m"[39m
    [32m--> [39m[32m120[39m     [38;5;28;01mtry[39;00m: [38;5;28;01mreturn[39;00m dict2obj([38;5;28;01mawait[39;00m [38;5;28mself[39m.client.request([38;5;28mself[39m.verb, url, headers=headers, params=query, json=body, **kwargs))
    [32m    121[39m     [38;5;28;01mexcept[39;00m [38;5;167;01mException[39;00m [38;5;28;01mas[39;00m e: [38;5;28mself[39m._raise_with_context(e, endpoint=[33m'[39m[33m'[39m, route=route, query=query, body=body)

    [36mFile [39m[32m~/aai-ws/fasttransport/fasttransport/core.py:58[39m, in [36mAsyncTransport.request[39m[34m(self, method, url, headers, params, json, data, files, content, raw)[39m
    [32m     56[39m [33m"[39m[33mExecute a request and decode JSON/text/binary response.[39m[33m"[39m
    [32m     57[39m [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m [38;5;28mself[39m._client() [38;5;28;01mas[39;00m client:
    [32m---> [39m[32m58[39m     resp = [38;5;28;01mawait[39;00m client.request(method, url, headers=[38;5;28mself[39m._request_headers(headers, files=files),
    [32m     59[39m         params=params, json=json, data=data, files=files, content=content)
    [32m     60[39m     [38;5;28;01mtry[39;00m: resp.raise_for_status()
    [32m     61[39m     [38;5;28;01mexcept[39;00m httpx2.HTTPStatusError [38;5;28;01mas[39;00m e:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1635[39m, in [36mAsyncClient.request[39m[34m(self, method, url, content, data, files, json, params, headers, cookies, auth, follow_redirects, timeout, extensions)[39m
    [32m   1620[39m     warnings.warn(message, [38;5;167;01mDeprecationWarning[39;00m, stacklevel=[32m2[39m)
    [32m   1622[39m request = [38;5;28mself[39m.build_request(
    [32m   1623[39m     method=method,
    [32m   1624[39m     url=url,
    [32m   (...)[39m[32m   1633[39m     extensions=extensions,
    [32m   1634[39m )
    [32m-> [39m[32m1635[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m.send(request, auth=auth, follow_redirects=follow_redirects)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1818[39m, in [36mAsyncClient.send[39m[34m(self, request, stream, auth, follow_redirects)[39m
    [32m   1814[39m [38;5;28mself[39m._set_timeout(request)
    [32m   1816[39m auth = [38;5;28mself[39m._build_request_auth(request, auth)
    [32m-> [39m[32m1818[39m response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_handling_auth(
    [32m   1819[39m     request,
    [32m   1820[39m     auth=auth,
    [32m   1821[39m     follow_redirects=follow_redirects,
    [32m   1822[39m     history=[],
    [32m   1823[39m )
    [32m   1824[39m [38;5;28;01mtry[39;00m:
    [32m   1825[39m     [38;5;28;01mif[39;00m [38;5;129;01mnot[39;00m stream:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1846[39m, in [36mAsyncClient._send_handling_auth[39m[34m(self, request, auth, follow_redirects, history)[39m
    [32m   1843[39m request = [38;5;28;01mawait[39;00m auth_flow.[34m__anext__[39m()
    [32m   1845[39m [38;5;28;01mwhile[39;00m [38;5;28;01mTrue[39;00m:
    [32m-> [39m[32m1846[39m     response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_handling_redirects(
    [32m   1847[39m         request,
    [32m   1848[39m         follow_redirects=follow_redirects,
    [32m   1849[39m         history=history,
    [32m   1850[39m     )
    [32m   1851[39m     [38;5;28;01mtry[39;00m:
    [32m   1852[39m         [38;5;28;01mtry[39;00m:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1881[39m, in [36mAsyncClient._send_handling_redirects[39m[34m(self, request, follow_redirects, history)[39m
    [32m   1878[39m [38;5;28;01mfor[39;00m hook [38;5;129;01min[39;00m [38;5;28mself[39m._event_hooks[[33m"[39m[33mrequest[39m[33m"[39m]:
    [32m   1879[39m     [38;5;28;01mawait[39;00m hook(request)
    [32m-> [39m[32m1881[39m response = [38;5;28;01mawait[39;00m [38;5;28mself[39m._send_single_request(request)
    [32m   1882[39m [38;5;28;01mtry[39;00m:
    [32m   1883[39m     [38;5;28;01mfor[39;00m hook [38;5;129;01min[39;00m [38;5;28mself[39m._event_hooks[[33m"[39m[33mresponse[39m[33m"[39m]:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_client.py:1915[39m, in [36mAsyncClient._send_single_request[39m[34m(self, request)[39m
    [32m   1912[39m     [38;5;28;01mraise[39;00m [38;5;167;01mRuntimeError[39;00m([33m"[39m[33mAttempted to send a sync request with an AsyncClient instance.[39m[33m"[39m)
    [32m   1914[39m [38;5;28;01mwith[39;00m request_context(request=request):
    [32m-> [39m[32m1915[39m     response = [38;5;28;01mawait[39;00m transport.handle_async_request(request)
    [32m   1917[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(response.stream, AsyncByteStream)
    [32m   1918[39m response.request = request

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpx2/_transports/default.py:388[39m, in [36mAsyncHTTPTransport.handle_async_request[39m[34m(self, request)[39m
    [32m    375[39m req = httpcore2.Request(
    [32m    376[39m     method=request.method,
    [32m    377[39m     url=httpcore2.URL(
    [32m   (...)[39m[32m    385[39m     extensions=request.extensions,
    [32m    386[39m )
    [32m    387[39m [38;5;28;01mwith[39;00m map_httpcore_exceptions():
    [32m--> [39m[32m388[39m     resp = [38;5;28;01mawait[39;00m [38;5;28mself[39m._pool.handle_async_request(req)
    [32m    390[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(resp.stream, typing.AsyncIterable)
    [32m    392[39m [38;5;28;01mreturn[39;00m Response(
    [32m    393[39m     status_code=resp.status,
    [32m    394[39m     headers=resp.headers,
    [32m    395[39m     stream=AsyncResponseStream(resp.stream),
    [32m    396[39m     extensions=resp.extensions,
    [32m    397[39m )

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection_pool.py:242[39m, in [36mAsyncConnectionPool.handle_async_request[39m[34m(self, request)[39m
    [32m    239[39m         closing = [38;5;28mself[39m._assign_requests_to_connections()
    [32m    241[39m     [38;5;28;01mawait[39;00m [38;5;28mself[39m._close_connections(closing)
    [32m--> [39m[32m242[39m     [38;5;28;01mraise[39;00m exc [38;5;28;01mfrom[39;00m[38;5;250m [39m[38;5;28;01mNone[39;00m
    [32m    244[39m [38;5;66;03m# Return the response. Note that in this case we still have to manage[39;00m
    [32m    245[39m [38;5;66;03m# the point at which the response is closed.[39;00m
    [32m    246[39m [38;5;28;01massert[39;00m [38;5;28misinstance[39m(response.stream, typing.AsyncIterable)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection_pool.py:224[39m, in [36mAsyncConnectionPool.handle_async_request[39m[34m(self, request)[39m
    [32m    220[39m connection = [38;5;28;01mawait[39;00m pool_request.wait_for_connection(timeout=timeout)
    [32m    222[39m [38;5;28;01mtry[39;00m:
    [32m    223[39m     [38;5;66;03m# Send the request on the assigned connection.[39;00m
    [32m--> [39m[32m224[39m     response = [38;5;28;01mawait[39;00m connection.handle_async_request(pool_request.request)
    [32m    225[39m [38;5;28;01mexcept[39;00m ConnectionNotAvailable:
    [32m    226[39m     [38;5;66;03m# In some cases a connection may initially be available to[39;00m
    [32m    227[39m     [38;5;66;03m# handle a request, but then become unavailable.[39;00m
    [32m    228[39m     [38;5;66;03m#[39;00m
    [32m    229[39m     [38;5;66;03m# In this case we clear the connection and try again.[39;00m
    [32m    230[39m     pool_request.clear_connection()

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/connection.py:96[39m, in [36mAsyncHTTPConnection.handle_async_request[39m[34m(self, request)[39m
    [32m     93[39m     [38;5;28mself[39m._connect_failed = [38;5;28;01mTrue[39;00m
    [32m     94[39m     [38;5;28;01mraise[39;00m exc
    [32m---> [39m[32m96[39m [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._connection.handle_async_request(request)

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:125[39m, in [36mAsyncHTTP11Connection.handle_async_request[39m[34m(self, request)[39m
    [32m    123[39m     [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m Trace([33m"[39m[33mresponse_closed[39m[33m"[39m, logger, request) [38;5;28;01mas[39;00m trace:
    [32m    124[39m         [38;5;28;01mawait[39;00m [38;5;28mself[39m._response_closed()
    [32m--> [39m[32m125[39m [38;5;28;01mraise[39;00m exc

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:97[39m, in [36mAsyncHTTP11Connection.handle_async_request[39m[34m(self, request)[39m
    [32m     88[39m     [38;5;28;01mpass[39;00m
    [32m     90[39m [38;5;28;01masync[39;00m [38;5;28;01mwith[39;00m Trace([33m"[39m[33mreceive_response_headers[39m[33m"[39m, logger, request, kwargs) [38;5;28;01mas[39;00m trace:
    [32m     91[39m     (
    [32m     92[39m         http_version,
    [32m     93[39m         status,
    [32m     94[39m         reason_phrase,
    [32m     95[39m         headers,
    [32m     96[39m         trailing_data,
    [32m---> [39m[32m97[39m     ) = [38;5;28;01mawait[39;00m [38;5;28mself[39m._receive_response_headers(**kwargs)
    [32m     98[39m     trace.return_value = (
    [32m     99[39m         http_version,
    [32m    100[39m         status,
    [32m    101[39m         reason_phrase,
    [32m    102[39m         headers,
    [32m    103[39m     )
    [32m    105[39m network_stream = [38;5;28mself[39m._network_stream

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:167[39m, in [36mAsyncHTTP11Connection._receive_response_headers[39m[34m(self, request)[39m
    [32m    164[39m timeout = timeouts.get([33m"[39m[33mread[39m[33m"[39m, [38;5;28;01mNone[39;00m)
    [32m    166[39m [38;5;28;01mwhile[39;00m [38;5;28;01mTrue[39;00m:
    [32m--> [39m[32m167[39m     event = [38;5;28;01mawait[39;00m [38;5;28mself[39m._receive_event(timeout=timeout)
    [32m    168[39m     [38;5;28;01mif[39;00m [38;5;28misinstance[39m(event, h11.Response):
    [32m    169[39m         [38;5;28;01mbreak[39;00m

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_async/http11.py:200[39m, in [36mAsyncHTTP11Connection._receive_event[39m[34m(self, timeout)[39m
    [32m    197[39m     event = [38;5;28mself[39m._h11_state.next_event()
    [32m    199[39m [38;5;28;01mif[39;00m event [38;5;129;01mis[39;00m h11.NEED_DATA:
    [32m--> [39m[32m200[39m     data = [38;5;28;01mawait[39;00m [38;5;28mself[39m._network_stream.read([38;5;28mself[39m.READ_NUM_BYTES, timeout=timeout)
    [32m    202[39m     [38;5;66;03m# If we feed this case through h11 we'll raise an exception like:[39;00m
    [32m    203[39m     [38;5;66;03m#[39;00m
    [32m    204[39m     [38;5;66;03m#     httpcore2.RemoteProtocolError: can't handle event type[39;00m
    [32m   (...)[39m[32m    208[39m     [38;5;66;03m# perspective. Instead we handle this case distinctly and treat[39;00m
    [32m    209[39m     [38;5;66;03m# it as a ConnectError.[39;00m
    [32m    210[39m     [38;5;28;01mif[39;00m data == [33mb[39m[33m"[39m[33m"[39m [38;5;129;01mand[39;00m [38;5;28mself[39m._h11_state.their_state == h11.SEND_RESPONSE:

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/httpcore2/_backends/anyio.py:37[39m, in [36mAnyIOStream.read[39m[34m(self, max_bytes, timeout)[39m
    [32m     35[39m [38;5;28;01mwith[39;00m anyio.fail_after(timeout):
    [32m     36[39m     [38;5;28;01mtry[39;00m:
    [32m---> [39m[32m37[39m         [38;5;28;01mreturn[39;00m [38;5;28;01mawait[39;00m [38;5;28mself[39m._stream.receive(max_bytes=max_bytes)
    [32m     38[39m     [38;5;28;01mexcept[39;00m anyio.EndOfStream:  [38;5;66;03m# pragma: no cover[39;00m
    [32m     39[39m         [38;5;28;01mreturn[39;00m [33mb[39m[33m"[39m[33m"[39m

    [36mFile [39m[32m~/aai-ws/.venv/lib/python3.13/site-packages/anyio/_backends/_asyncio.py:1337[39m, in [36mSocketStream.receive[39m[34m(self, max_bytes)[39m
    [32m   1331[39m [38;5;28;01mif[39;00m (
    [32m   1332[39m     [38;5;129;01mnot[39;00m [38;5;28mself[39m._protocol.read_event.is_set()
    [32m   1333[39m     [38;5;129;01mand[39;00m [38;5;129;01mnot[39;00m [38;5;28mself[39m._transport.is_closing()
    [32m   1334[39m     [38;5;129;01mand[39;00m [38;5;129;01mnot[39;00m [38;5;28mself[39m._protocol.is_at_eof
    [32m   1335[39m ):
    [32m   1336[39m     [38;5;28mself[39m._transport.resume_reading()
    [32m-> [39m[32m1337[39m     [38;5;28;01mawait[39;00m [38;5;28mself[39m._protocol.read_event.wait()
    [32m   1338[39m     [38;5;28mself[39m._transport.pause_reading()
    [32m   1339[39m [38;5;28;01melse[39;00m:

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/asyncio/locks.py:213[39m, in [36mEvent.wait[39m[34m(self)[39m
    [32m    211[39m [38;5;28mself[39m._waiters.append(fut)
    [32m    212[39m [38;5;28;01mtry[39;00m:
    [32m--> [39m[32m213[39m     [38;5;28;01mawait[39;00m fut
    [32m    214[39m     [38;5;28;01mreturn[39;00m [38;5;28;01mTrue[39;00m
    [32m    215[39m [38;5;28;01mfinally[39;00m:

    [31mCancelledError[39m: 

``` python
print((await read_msg(n=0, id=_edit_id, nums=True))['content'])
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True, start_line=2, end_line=3))['content'])
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L765"
target="_blank" style="float:right; font-size:smaller">source</a>

### run_msg

``` python
async def run_msg(
    ids:str=None, # Comma-separated ids of message(s) to execute
    dname:str='', # Running dialog to get info for; defaults to current dialog. (Note dialog *must* be running for this function)
):
```

*Adds a message to the run queue. Use read_msg to see the output once it
runs.*

``` python
codeid = (await read_msg())['id']
```

``` python
await run_msg(codeid)
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L773"
target="_blank" style="float:right; font-size:smaller">source</a>

### copy_msgs

``` python
async def copy_msgs(
    ids:str=None, # Comma-separated ids of message(s) to copy
    cut:bool=False, # Cut message(s)? (If not, copies)
    dname:str='', # Running dialog to copy messages from; defaults to current dialog. (Note dialog *must* be running for this function)
)->dict:
```

*Add `ids` to clipboard.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L784"
target="_blank" style="float:right; font-size:smaller">source</a>

### paste_msgs

``` python
async def paste_msgs(
    id:str=None, # Message id to paste next to
    after:bool=True, # Paste after id? (If not, pastes before)
    dname:str='', # Running dialog to copy messages from; defaults to current dialog. (Note dialog *must* be running for this function)
)->dict:
```

*Paste clipboard msg(s) after/before the current selected msg (id).*

``` python
await copy_msgs(codeid)
```

``` python
with expect_fail(DialogAPIError, contains='Dialog /dlg/nonexistent.ipynb may not be running'):
    await copy_msgs('_fake', dname='/dlg/nonexistent.ipynb')
```

``` python
tgt = (await read_msg())['id']
```

``` python
await paste_msgs(tgt)
```

``` python
# TODO fix so this passes
# with expect_fail(DialogAPIError, contains='Dialog /dlg/nonexistent may not be running'):
#     await paste_msgs('_fake', dname='/dlg/nonexistent')
```

``` python
newmsg = await read_msg(1, id=tgt)
newmsg['content']
```

``` python
await del_msgs(newmsg['id'])
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L796"
target="_blank" style="float:right; font-size:smaller">source</a>

### enable_mermaid

``` python
def enable_mermaid():
```

``` python
enable_mermaid()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L810"
target="_blank" style="float:right; font-size:smaller">source</a>

### mermaid

``` python
def mermaid(
    code, cls:str='mermaid', **kwargs
):
```

*A mermaid diagram*

``` python
mermaid('graph LR; A[Start] --> B[Process]; B --> C[End];')
```

You can also add to a note:

    ```mermaid
    graph LR
    A[Start] --> B[Process]
    B --> C[End]
    ```

This renders as:

``` mermaid
graph LR
A[Start] --> B[Process]
B --> C[End]
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L815"
target="_blank" style="float:right; font-size:smaller">source</a>

### toggle_header

``` python
async def toggle_header(
    id:str, # id of markdown header note message to toggle collapsed state
    dname:str='', # Dialog to toggle in; defaults to current dialog
)->dict:
```

*Toggle collapsed header state for `id`*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L824"
target="_blank" style="float:right; font-size:smaller">source</a>

### toggle_bookmark

``` python
async def toggle_bookmark(
    id:str, # id of message to toggle bookmark on
    n:int, # Bookmark number (1-9)
    dname:str='', # Dialog to set bookmark in; defaults to current dialog
)->dict:
```

*Toggle numbered bookmark (1-9) on a message, clearing it from any other
message when setting*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L838"
target="_blank" style="float:right; font-size:smaller">source</a>

### toggle_export

``` python
async def toggle_export(
    ids:str | list, # Message id(s) to toggle (comma-separated str, or list)
    dname:str='', # Dialog to get info for; defaults to current dialog
)->dict:
```

*Toggle the nbdev `export` directive (meta form) on each message,
independently*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L854"
target="_blank" style="float:right; font-size:smaller">source</a>

### toggle_comment

``` python
async def toggle_comment(
    id:str, # id of code message (or comma-separated ids) to toggle comments on
    dname:str='', # Dialog to toggle comments in; defaults to current dialog
)->dict:
```

*Toggle line comments on code message(s). If any lines are uncommented,
comments all; otherwise uncomments all.*

``` python
await toggle_comment(codeid) # comment
await toggle_comment(codeid) # uncomment
```

``` python
with expect_fail(contains='such file'):
    await toggle_comment('_fake', dname='/dlg/nonexistent.ipynb')
```

#### test header

#### header end

``` python
hdid = (await read_msg())['id']
```

``` python
await toggle_header(hdid)
```

``` python
with expect_fail(APIError, contains='no such file'):
    await toggle_header('_fake', dname='/dlg/nonexistent.ipynb')
```

### Dlg conveniences

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L867"
target="_blank" style="float:right; font-size:smaller">source</a>

### url2note

``` python
async def url2note(
    url:str, # URL to read
    extract_section:bool=True, # If url has an anchor, return only that section
    selector:str=None, # Select section(s) using BeautifulSoup.select (overrides extract_section)
    ai_img:bool=True, # Make images visible to the AI
    split_re:str='', # Regex to split content into multiple notes, set to '' for single note
):
```

*Read URL as markdown, and add note(s) below current message with the
result*

``` python
_id = await url2note('https://docs.python.org')
```

``` python
await del_msgs(_id)
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L881"
target="_blank" style="float:right; font-size:smaller">source</a>

### create_or_run_dialog

``` python
async def create_or_run_dialog(
    name:str, # Name/path of the dialog (relative to current dialog's folder, or absolute if starts with '/')
    template:bool=True, # Include TEMPLATE.ipynb files when creating a new dialog
):
```

*Create a new dialog, or set an existing one running*

``` python
await create_or_run_dialog('test_dialog.ipynb')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L890"
target="_blank" style="float:right; font-size:smaller">source</a>

### restart_dialog

``` python
async def restart_dialog(
    name:str, # Name/path of the dialog (relative to current dialog's folder, or absolute if starts with '/')
):
```

*Restart a dialog’s kernel, starting the dialog first if needed*

``` python
await restart_dialog('test_dialog.ipynb')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L898"
target="_blank" style="float:right; font-size:smaller">source</a>

### stop_dialog

``` python
async def stop_dialog(
    name:str, # Name/path of the dialog (relative to current dialog's folder, or absolute if starts with '/')
):
```

*Stop a running dialog kernel*

``` python
await stop_dialog('test_dialog.ipynb')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L906"
target="_blank" style="float:right; font-size:smaller">source</a>

### rm_dialog

``` python
async def rm_dialog(
    name:str, # Name/path of the dialog to delete (relative to current dialog's folder, or absolute if starts with '/')
):
```

*Delete a dialog (or folder) and associated records, stopping the kernel
if running*

``` python
await rm_dialog('test_dialog.ipynb')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L914"
target="_blank" style="float:right; font-size:smaller">source</a>

### run_code_interactive

``` python
async def run_code_interactive(
    code:str, # Code to have user run
):
```

*Insert code into user’s dialog and request for the user to run it. Use
other functions where possible,* but if they can not find needed
information, *ALWAYS* use this instead of guessing or giving up.
IMPORTANT: This tool is TERMINAL - after calling it, you MUST stop all
tool usage and wait for user response. Never call additional tools after
this one.

[`Message.execute`](https://AnswerDotAI.github.io/dialoghelper/core.html#message.execute)
and
[`Dialog.execute`](https://AnswerDotAI.github.io/dialoghelper/core.html#dialog.execute)
run messages in the current Solveit kernel. They replace aidialog’s
default `CaptureShell` execution, which uses a temporary shell. The
Solveit server handles these runs like user-initiated runs, including
attribution, the spinner, streamed card updates, and saving to disk.
Awaiting `execute` returns the messages’ outputs.

A tool can call `execute` while its own message is still running. Adding
that work to the main shell’s serial queue would deadlock. `execute`
routes the nested work through the kernel’s persistent `sidecar`
subshell instead. The sidecar runs it on a separate serial queue while
the calling message waits.

When `py` runs with safepyrun’s audit checks, the nested execution
remains inside that audit window. `execute` does not add a separate
sandbox.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L938"
target="_blank" style="float:right; font-size:smaller">source</a>

### Dialog.execute

``` python
async def execute(
    *ids, above:bool=False, # Include each matched cell and all cells above it?
    below:bool=False, # Include each matched cell and all cells below it?
    all:bool=False, # Include all code cells (ignores `msgids`)?
    exported:bool=False, # Only cells with `#| export` or `#| exports`?
    default_eval:bool=True, # Participation default when neither the cell nor the notebook has an `eval` directive
    ignore_eval:bool=False, # Skip `eval` filtering entirely: every selected cell runs
):
```

*Run the selected code messages through Solveit and return their
outputs*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L932"
target="_blank" style="float:right; font-size:smaller">source</a>

### Message.execute

``` python
async def execute():
```

*Run this message through Solveit and return its output*

``` python
d = Dialog(name='mm')
m = d.mk_message('x', msg_type='note', meta=dict(hide_input=True, bookmark=3))
test_eq((m.i_collapsed, m.bookmark), (True, 3))  # declared fields promote at construction
m.i_collapsed = 1
test_eq(m.cell_meta(), dict(hide_input=True, bookmark=3))  # ints demote as booleans, keys per the file convention
```

## Text Edit

``` python
await msg_insert_line(_edit_id, 0, 'This should go to the first line')
await msg_insert_line(_edit_id, 3, 'This should go to the 4th line')
print(await msg_insert_line(_edit_id, 5, 'This should go to the last line'))
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True))['content'])
```

``` python
print(await msg_str_replace(_edit_id, 'This should go to the first line', 'This should go to the 1st line'))
```

``` python
for f in (msg_str_replace, msg_strs_replace):
    assert 'use_regex' in inspect.signature(f).parameters
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True))['content'])
```

``` python
print(await msg_strs_replace(_edit_id, ['This is a multiline message.', 'This should go to the last line'], ['5th line', 'last line']))
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True))['content'])
```

``` python
print(await msg_replace_lines(_edit_id, 2, 4,'line 2\nline 3\nline 4\n'))
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True))['content'])
```

``` python
print(await msg_del_lines(_edit_id, 2, 4))
```

``` python
print((await read_msg(n=0, id=_edit_id, nums=True)).content)
```

``` python
await del_msgs(_edit_id)
```

``` python
id1 = await add_msg('hello world')
id2 = await add_msg('hello there', id=id1)
```

``` python
results = await msg_str_replace([id1, id2], 'hello', 'hi')
print(results)
```

``` python
await del_msgs(id1)
await del_msgs(id2)
```

## Help

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L1003"
target="_blank" style="float:right; font-size:smaller">source</a>

### solveit_docs

``` python
async def solveit_docs():
```

*Full reference documentation for Solveit - use this to answer questions
about how to use Solveit.* **NB**: The whole docs fit in LLM context, so
read the whole thing, don’t search/filter it. *Always* re-run rather
than relying on truncated history or assumptions.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L1013"
target="_blank" style="float:right; font-size:smaller">source</a>

### dialog_link

``` python
def dialog_link(
    dname:str='', # Dialog to link to (relative to current dialog's folder, or absolute if starts with '/'); empty for in-page anchor link
    msg_id:str=None, # Optional message id to scroll to
):
```

*Return an IPython HTML link to open a dialog in Solveit.* After calling
this, output the resulting HTML anchor tag exactly as returned—do not
wrap in a fenced code block or convert to markdown link format.

``` python
dialog_link(msg_id='a7d82acd')
```

``` python
dialog_link(dname='/CRAFT.ipynb')
```

``` python
dialog_link(dname='/CRAFT.ipynb', msg_id='ce727fd8')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/core.py#L1029"
target="_blank" style="float:right; font-size:smaller">source</a>

### spawn_agent

``` python
async def spawn_agent(
    prompt:str
):
```

*Spawn a subagent to complete a task defined by `prompt`. Must be run as
a tool - not from Python.* The subagent’s context and tools is defined
by the parent prompt’s history

testing
