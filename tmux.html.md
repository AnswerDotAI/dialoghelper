# tmux


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## SSH

All capture and list functions accept SSH kwargs to target a remote
machine: use `host` for an SSH alias, or `ip` + `user` + optional
`keyfile`.

``` python
pane(host='hack')
windows(ip='1.2.3.4', user='ubuntu', keyfile='~/.ssh/id_rsa')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### shell_ret

``` python
def shell_ret(
    cmd:str, capture_output:bool=True, text:bool=True, ret:bool=True, *, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Run shell command locally or over ssh (use host for alias, or
ip/user/keyfile)*

``` python
print(shell_ret('du -sh'))
```

    416K    .

``` python
print(shell_ret('du -sh', host='hack'))
```

    KeyboardInterrupt: 
    [31m---------------------------------------------------------------------------[39m
    [31mKeyboardInterrupt[39m                         Traceback (most recent call last)
    [36mCell[39m[36m [39m[32mIn[17][39m[32m, line 1[39m
    [32m----> [39m[32m1[39m print(shell_ret([33m'du -sh'[39m, host=[33m'hack'[39m))

    [36mCell[39m[36m [39m[32mIn[13][39m[32m, line 8[39m, in [36mshell_ret[39m[34m(cmd, capture_output, text, ret, **kwargs)[39m
    [32m      4[39m     [33m"Run shell command locally or over ssh (use host for alias, or ip/user/keyfile)"[39m
    [32m      5[39m     host, ip, user, keyfile = kwargs.pop([33m'host'[39m, [38;5;28;01mNone[39;00m), kwargs.pop([33m'ip'[39m, [38;5;28;01mNone[39;00m), kwargs.pop([33m'user'[39m, [38;5;28;01mNone[39;00m), kwargs.pop([33m'keyfile'[39m, [38;5;28;01mNone[39;00m)
    [32m      6[39m     [38;5;28;01mif[39;00m host: cmd = f"echo '{cmd}' | ssh -A {host} 'bash -ls'"
    [32m      7[39m     [38;5;28;01melif[39;00m ip: cmd = f"echo '{cmd}' | ssh {f'-i {keyfile} ' [38;5;28;01mif[39;00m keyfile [38;5;28;01melse[39;00m [33m''[39m}-A {user}@{ip} 'bash -ls'"
    [32m----> [39m[32m8[39m     o = subprocess.run(cmd, shell=[38;5;28;01mTrue[39;00m, text=text, capture_output=capture_output, **kwargs)
    [32m      9[39m     [38;5;28;01mreturn[39;00m (o.stdout [38;5;28;01mor[39;00m o.stderr) [38;5;28;01mif[39;00m ret [38;5;28;01melse[39;00m o

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/subprocess.py:556[39m, in [36mrun[39m[34m(input, capture_output, timeout, check, *popenargs, **kwargs)[39m
    [32m    554[39m [38;5;28;01mwith[39;00m Popen(*popenargs, **kwargs) [38;5;28;01mas[39;00m process:
    [32m    555[39m     [38;5;28;01mtry[39;00m:
    [32m--> [39m[32m556[39m         stdout, stderr = [30;43mprocess[39;49m[30;43m.[39;49m[30;43mcommunicate[39;49m[30;43m([39;49m[30;43minput[39;49m[30;43m,[39;49m[30;43m [39;49m[30;43mtimeout[39;49m[30;43m=[39;49m[30;43mtimeout[39;49m[30;43m)[39;49m
    [32m    557[39m     [38;5;28;01mexcept[39;00m TimeoutExpired [38;5;28;01mas[39;00m exc:
    [32m    558[39m         process.kill()

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/subprocess.py:1222[39m, in [36mPopen.communicate[39m[34m(self, input, timeout)[39m
    [32m   1219[39m     endtime = [38;5;28;01mNone[39;00m
    [32m   1221[39m [38;5;28;01mtry[39;00m:
    [32m-> [39m[32m1222[39m     stdout, stderr = [30;43mself[39;49m[30;43m.[39;49m[30;43m_communicate[39;49m[30;43m([39;49m[30;43minput[39;49m[30;43m,[39;49m[30;43m [39;49m[30;43mendtime[39;49m[30;43m,[39;49m[30;43m [39;49m[30;43mtimeout[39;49m[30;43m)[39;49m
    [32m   1223[39m [38;5;28;01mexcept[39;00m [38;5;167;01mKeyboardInterrupt[39;00m:
    [32m   1224[39m     [38;5;66;03m# https://bugs.python.org/issue25942[39;00m
    [32m   1225[39m     [38;5;66;03m# See the detailed comment in .wait().[39;00m
    [32m   1226[39m     [38;5;28;01mif[39;00m timeout [38;5;129;01mis[39;00m [38;5;129;01mnot[39;00m [38;5;28;01mNone[39;00m:

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/subprocess.py:2154[39m, in [36mPopen._communicate[39m[34m(self, input, endtime, orig_timeout)[39m
    [32m   2147[39m     [38;5;28mself[39m._check_timeout(endtime, orig_timeout,
    [32m   2148[39m                         stdout, stderr,
    [32m   2149[39m                         skip_check_and_raise=[38;5;28;01mTrue[39;00m)
    [32m   2150[39m     [38;5;28;01mraise[39;00m [38;5;167;01mRuntimeError[39;00m(  [38;5;66;03m# Impossible :)[39;00m
    [32m   2151[39m         [33m'[39m[33m_check_timeout(..., skip_check_and_raise=True) [39m[33m'[39m
    [32m   2152[39m         [33m'[39m[33mfailed to raise TimeoutExpired.[39m[33m'[39m)
    [32m-> [39m[32m2154[39m ready = [30;43mselector[39;49m[30;43m.[39;49m[30;43mselect[39;49m[30;43m([39;49m[30;43mtimeout[39;49m[30;43m)[39;49m
    [32m   2155[39m [38;5;28mself[39m._check_timeout(endtime, orig_timeout, stdout, stderr)
    [32m   2157[39m [38;5;66;03m# XXX Rewrite these to use non-blocking I/O on the file[39;00m
    [32m   2158[39m [38;5;66;03m# objects; they are no longer using C stdio![39;00m

    [36mFile [39m[32m~/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/lib/python3.13/selectors.py:398[39m, in [36m_PollLikeSelector.select[39m[34m(self, timeout)[39m
    [32m    396[39m ready = []
    [32m    397[39m [38;5;28;01mtry[39;00m:
    [32m--> [39m[32m398[39m     fd_event_list = [30;43mself[39;49m[30;43m.[39;49m[30;43m_selector[39;49m[30;43m.[39;49m[30;43mpoll[39;49m[30;43m([39;49m[30;43mtimeout[39;49m[30;43m)[39;49m
    [32m    399[39m [38;5;28;01mexcept[39;00m [38;5;167;01mInterruptedError[39;00m:
    [32m    400[39m     [38;5;28;01mreturn[39;00m ready

    [36mFile [39m[32m~/aai-ws/kernmini/kernmini/kernel.py:821[39m, in [36mMiniKernel.handle_sigint[39m[34m(self, signum, frame)[39m
    [32m    819[39m [38;5;28;01mif[39;00m parent.cancel_async_execution(wake=[38;5;28;01mTrue[39;00m): [38;5;28;01mreturn[39;00m
    [32m    820[39m [38;5;28;01mif[39;00m [38;5;129;01mnot[39;00m parent.sync_executing.is_set(): [38;5;28;01mreturn[39;00m
    [32m--> [39m[32m821[39m [38;5;28;01mraise[39;00m [38;5;167;01mKeyboardInterrupt[39;00m

    [31mKeyboardInterrupt[39m: 

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L63"
target="_blank" style="float:right; font-size:smaller">source</a>

### set_default_history

``` python
def set_default_history(
    n:int
):
```

*Set the default number of lines to capture from tmux history*

### pane

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L70"
target="_blank" style="float:right; font-size:smaller">source</a>

### pane

``` python
def pane(
    n:int=None, # Number of scrollback lines to capture, in addition to visible area (None uses default_tmux_lines, which is 500 if not otherwise set)
    pane:int | str=None, # Pane number to capture from (accepts both integers for window IDs and "%{int}" for global pane IDs)
    session:str=None, # Session name to target
    window:int=None, # Window number to target
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab the tmux history in plain text*

``` python
# print(pane(1))
```

``` python
# print(pane(1, host='hack'))
```

### list_panes

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L88"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_panes

``` python
def list_panes(
    session:str=None, # Session name to list panes from
    window:int=None, # Window number to list panes from
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List panes for a session/window (or current if none specified)*

``` python
print(list_panes(window=0))
```

``` python
print(list_panes(window=0, host='hack'))
```

### panes

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L106"
target="_blank" style="float:right; font-size:smaller">source</a>

### panes

``` python
def panes(
    session:str=None, # Session name to target
    window:int=None, # Window number to target
    n:int=None, # Number of scrollback lines to capture
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in a session/window*

``` python
from pprint import pprint
```

``` python
# pprint(panes(window=0, n=10))
```

``` python
# pprint(panes(n=10, host='hack'))
```

### list_windows

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L119"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_windows

``` python
def list_windows(
    session:str=None, # Session name to list windows from
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List all windows in a session*

``` python
print(list_windows())
```

``` python
print(list_windows(host='hack'))
```

### windows

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L136"
target="_blank" style="float:right; font-size:smaller">source</a>

### windows

``` python
def windows(
    session:str=None, # Session name to target
    n:int=None, # Number of scrollback lines to capture
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in all windows of a session as
`{'win_num:name': {pane_num: content}}`*

``` python
# pprint(windows())
```

``` python
# pprint(windows(host='hack'))
```

### list_sessions

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L147"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_sessions

``` python
def list_sessions(
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List all tmux sessions*

``` python
print(list_sessions())
```

``` python
print(list_sessions(host='hack'))
```

### sessions

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L158"
target="_blank" style="float:right; font-size:smaller">source</a>

### sessions

``` python
def sessions(
    n:int=None, # Number of scrollback lines to capture
    *, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in all windows of all sessions as
`{session: {'win_num:name': {pane_num: content}}}`*

``` python
# pprint(sessions())
```

``` python
# pprint(sessions(host='hack'))
```

### Searching nested results

[`windows()`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#windows)
and
[`sessions()`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#sessions)
return nested dicts. Use
[`flatten_dict`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#flatten_dict)
to search them as `(path, content)` tuples, where `path` identifies the
session/window/pane source. The `path` string uses `//` separators
showing the hierarchy, e.g. `"mysession//0:bash//1"` means session
`"mysession"`, window `0` named `"bash"`, pane `1`.

For example, find error lines across all tmux history:

``` python
errors = [(path, line)
          for path, content in flatten_dict(sessions(n=2000))
          for line in content.splitlines()
          if 'error' in line.lower()]
```

Or find panes containing a command you remember:

``` python
found = {path: content for path, content in flatten_dict(windows(n=2000)) if 'curl' in content}
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L167"
target="_blank" style="float:right; font-size:smaller">source</a>

### flatten_dict

``` python
def flatten_dict(
    d, parent_key:str='', sep:str='//'
):
```

*Flatten nested dict into list of (key_path, value) tuples*

``` python
nested = {
    'session1': {
        '0:bash': {0: 'some content', 1: 'more content'},
        '1:vim': {0: 'editing file'}
    }
}
for path, content in flatten_dict(nested): print(f"{path}: {content}")
```
