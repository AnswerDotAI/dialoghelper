# tmux


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## SSH

All capture and list functions accept SSH kwargs to target a remote
machine: use `host` for an SSH alias, or `ip` + `user` + optional
`keyfile`.

``` python
pane(host='hack')
windows(ip='1.2.3.4', user='ubuntu', keyfile='~/.ssh/id_rsa')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### shell_ret

``` python
def shell_ret(
    cmd:str, capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Run shell command locally or over ssh (use host for alias, or
ip/user/keyfile)*

``` python
print(shell_ret('du -sh'))
```

    340K    .

``` python
print(shell_ret('du -sh', host='hack'))
```

    33G .

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L63"
target="_blank" style="float:right; font-size:smaller">source</a>

### set_default_history

``` python
def set_default_history(
    n:int
):
```

*Set the default number of lines to capture from tmux history*

### pane

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L70"
target="_blank" style="float:right; font-size:smaller">source</a>

### pane

``` python
def pane(
    n:int=None, # Number of scrollback lines to capture, in addition to visible area (None uses default_tmux_lines, which is 500 if not otherwise set)
    pane:int | str=None, # Pane number to capture from (accepts both integers for window IDs and "%{int}" for global pane IDs)
    session:str=None, # Session name to target
    window:int=None, # Window number to target
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab the tmux history in plain text*

``` python
# print(pane(1))
```

``` python
# print(pane(1, host='hack'))
```

### list_panes

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L88"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_panes

``` python
def list_panes(
    session:str=None, # Session name to list panes from
    window:int=None, # Window number to list panes from
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List panes for a session/window (or current if none specified)*

``` python
print(list_panes(window=0))
```

    0: [93x28] [history 1847/2000, 722583 bytes] %3
    1: [94x28] [history 1830/2000, 856695 bytes] %2
    2: [93x27] [history 1922/2000, 760751 bytes] %5
    3: [188x56] [history 1905/2000, 767895 bytes] %1 (active)

``` python
print(list_panes(window=0, host='hack'))
```

    0: [91x23] [history 1873/2000, 4368020 bytes] %0
    1: [91x22] [history 1934/2000, 4586743 bytes] %3
    2: [178x46] [history 1804/2000, 2649814 bytes] %1 (active)
    3: [86x22] [history 1820/2000, 3509468 bytes] %2

### panes

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L106"
target="_blank" style="float:right; font-size:smaller">source</a>

### panes

``` python
def panes(
    session:str=None, # Session name to target
    window:int=None, # Window number to target
    n:int=None, # Number of scrollback lines to capture
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in a session/window*

``` python
from pprint import pprint
```

``` python
# pprint(panes(window=0, n=10))
```

``` python
# pprint(panes(n=10, host='hack'))
```

### list_windows

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L119"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_windows

``` python
def list_windows(
    session:str=None, # Session name to list windows from
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List all windows in a session*

``` python
print(list_windows())
```

    0: 2.1.222*Z (4 panes) [188x56] [layout 1b75,188x56,0,0[188x28,0,0{93x28,0,0,3,94x28,94,0,2},188x27,0,29{93x27,0,29,5,94x27,94,29,1}]] @0 (active)
    1: python3.13- (2 panes) [188x56] [layout 8f87,188x56,0,0{94x56,0,0,9,93x56,95,0,10}] @4

``` python
print(list_windows(host='hack'))
```

    0: bash*Z (4 panes) [178x46] [layout a39c,178x46,0,0{91x46,0,0[91x23,0,0,0,91x22,0,24,3],86x46,92,0[86x23,92,0,1,86x22,92,24,2]}] @0 (active)

### windows

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L136"
target="_blank" style="float:right; font-size:smaller">source</a>

### windows

``` python
def windows(
    session:str=None, # Session name to target
    n:int=None, # Number of scrollback lines to capture
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in all windows of a session as
`{'win_num:name': {pane_num: content}}`*

``` python
# pprint(windows())
```

``` python
# pprint(windows(host='hack'))
```

### list_sessions

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L147"
target="_blank" style="float:right; font-size:smaller">source</a>

### list_sessions

``` python
def list_sessions(
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*List all tmux sessions*

``` python
print(list_sessions())
```

    0: 2 windows (created Mon Jul 27 15:55:42 2026) (attached)
    play: 1 windows (created Tue Aug  4 21:23:52 2026)

``` python
print(list_sessions(host='hack'))
```

    0: 1 windows (created Thu Jun 25 23:08:32 2026)

### sessions

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L158"
target="_blank" style="float:right; font-size:smaller">source</a>

### sessions

``` python
def sessions(
    n:int=None, # Number of scrollback lines to capture
    capture_output:bool=True, text:bool=True, ret:bool=True, host:str=None, # Optional SSH Host
    ip:str=None, # Optional SSH IP
    user:str=None, # Optional SSH user
    keyfile:str=None, # Optional SSH keyfile
):
```

*Grab history from all panes in all windows of all sessions as
`{session: {'win_num:name': {pane_num: content}}}`*

``` python
# pprint(sessions())
```

``` python
# pprint(sessions(host='hack'))
```

### Searching nested results

[`windows()`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#windows)
and
[`sessions()`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#sessions)
return nested dicts. Use
[`flatten_dict`](https://AnswerDotAI.github.io/dialoghelper/tmux.html#flatten_dict)
to search them as `(path, content)` tuples, where `path` identifies the
session/window/pane source. The `path` string uses `//` separators
showing the hierarchy, e.g. `"mysession//0:bash//1"` means session
`"mysession"`, window `0` named `"bash"`, pane `1`.

For example, find error lines across all tmux history:

``` python
errors = [(path, line)
          for path, content in flatten_dict(sessions(n=2000))
          for line in content.splitlines()
          if 'error' in line.lower()]
```

Or find panes containing a command you remember:

``` python
found = {path: content for path, content in flatten_dict(windows(n=2000)) if 'curl' in content}
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/dialoghelper/blob/main/dialoghelper/tmux.py#L167"
target="_blank" style="float:right; font-size:smaller">source</a>

### flatten_dict

``` python
def flatten_dict(
    d, parent_key:str='', sep:str='//'
):
```

*Flatten nested dict into list of (key_path, value) tuples*

``` python
nested = {
    'session1': {
        '0:bash': {0: 'some content', 1: 'more content'},
        '1:vim': {0: 'editing file'}
    }
}
for path, content in flatten_dict(nested): print(f"{path}: {content}")
```

    session1//0:bash//0: some content
    session1//0:bash//1: more content
    session1//1:vim//0: editing file
